<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.mapper.BeddetailsMapper">
    <select id="selectBedDetailsVo" resultType="com.cy.vo.BedDetailsVo">
        select b.id,
               b.is_deleted,
               b.start_date,
               b.end_date,
               b.bed_details,
               b.customer_id,
               b.bed_id,
               c.customer_name,
               c.customer_sex
        from beddetails b
            left join customer c
                on b.customer_id = c.id
        <where>
            <if test="customerName != null and customerName != ''">
                and c.customer_name like concat('%',#{customerName},'%')
            </if>
            <if test="startDate != null and endDate != null">
                and b.start_date >= #{startDate} and #{endDate} >= b.start_date
            </if>
            and b.is_deleted = #{isDeleted}
        </where>
        order by b.end_date desc
        limit #{currentPage}, #{pageSize}
    </select>

    <select id="selectCountVo" resultType="com.cy.vo.CountVo">
        select
            count(*) as count
        from beddetails b
        left join customer c
        on b.customer_id = c.id
        <where>
            <if test="customerName != null and customerName != ''">
                and c.customer_name like concat('%',#{customerName},'%')
            </if>
            <if test="startDate != null and endDate != null">
                and b.start_date >= #{startDate} and #{endDate} >= b.start_date
            </if>
            and b.is_deleted = #{isDeleted}
        </where>
    </select>
</mapper>
